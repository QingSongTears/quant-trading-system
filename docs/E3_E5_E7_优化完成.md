# E3/E5/E7 优化完成报告 — 2026-06-21

## E5 ✅ SQLAlchemy Engine 单例

**新建文件**: `src/db/engine.py`
- `get_engine()` — 全局唯一 Engine 实例，线程安全（`threading.Lock`），首次调用自动加载 config.yaml
- `reset_engine_cache()` — 释放连接池，供定时任务使用
- 内置 SQLite WAL + foreign_keys pragma

**改造文件** (5 个核心模块):
| 文件 | 改动 |
|------|------|
| `src/models/repository.py` | `create_engine()` → `get_engine()`，移除 50 行 pragma 事件监听器 |
| `src/selection/pipeline.py` | 同上 |
| `src/strategies/v6_reversal_selection.py` | 同上 |
| `src/models/database.py` | `from __future__ import annotations` |
| 7 scripts | `create_engine()` → `get_engine()` / `read_sql()` |

**验证**: Singleton 成立 (`e1 is e2`)，`reset_engine_cache()` 重建生效，回测 + DataRepository 全部正常。

---

## E7 ✅ 统一类型注解

**执行策略**: 两阶段批量脚本
1. 批量替换 `Optional[X]→X|None`, `List[X]→list[X]`, `Dict[K,V]→dict[K,V]`, `Tuple[X]→tuple[X]`
2. 递归处理嵌套类型 (`Optional[dict[str, Any]]`)
3. 清理 `from typing import` 残留

**成果**:
- 68 个 `src/` .py 文件全部 `from __future__ import annotations`
- **0 个文件**残留 `Optional[`, `List[`, `Dict[`, `Tuple[` 作为类型标注
- 323 处类型替换 + 37 个文件清理 typing 导入

---

## E3 ✅ 统一直连 DB 访问 (部分完成)

**已完成 7 个脚本**，消除 sqlite3 直连：
| 脚本 | 改动 |
|------|------|
| `batch_backtest_v3.py` | `sqlite3.connect` → `read_sql()` via `get_engine()` |
| `verify_bull_model.py` | 同上 |
| `full_all_strategies_v3.py` | 4 个 `sqlite3.connect` → 统一 `read_sql()` |
| `show_db_stats.py` | `sqlite3.connect` → `read_sql()` |
| `daily_pred_train_v3.py` | `__import__("sqlite3")` → `pd.read_sql` via `get_engine()` |
| `full_bull_backtest.py` | `sqlite3.connect` → `read_sql()` |
| `export_7d_scores.py` | `sqlite3` + `create_engine` → `get_engine()` |

**残余 5 个** (低优先级，大型数据下载脚本):
`param_server.py`, `build_bull_sample_pool.py`, `download_fund_flow_full.py`, `download_institutional_data.py`, `import_lhb_institutional.py`

---

## 联动效应

E5 单例 Engine 让所有后续 DB 操作自动走统一连接池：
- E3 脚本全部通过 `get_engine()` 连接，无需手动管理连接关闭
- E7 类型标注与 E5 的 engine type hints 一致使用现代语法
